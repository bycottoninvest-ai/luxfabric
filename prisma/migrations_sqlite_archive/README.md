# Eski SQLite migratsiyalar (arxiv)

Bu papka **faqat tarix** uchun. Production endi **PostgreSQL** (`prisma/migrations/` + `provider = "postgresql"`).

Lokal `file:./dev.db` Vercelda ishlamaydi — Neon/Prisma Postgres ishlating.
Batafsil: `docs/VERCEL-DEPLOY.md`, `docs/PRODUCTION-REJA.md`.
